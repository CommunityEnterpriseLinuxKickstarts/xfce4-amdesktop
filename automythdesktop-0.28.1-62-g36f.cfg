##Config Src:
## http://www.livecd.ethz.ch/build.html
##Applied a few Montana Linux 7 live ideas.
# https://img.cs.montana.edu/linux/montanalinux



#modified parts and new development by AutoMyth Developers
#Licences: Existing + GPL2 on new/modifications.
#change local repos to your local or mirrors


lang en_US.UTF-8
keyboard us
timezone America/New_York 
auth --useshadow --enablemd5
selinux --permissive
#firewall --enabled
xconfig --startxonboot
part / --size 7300 --fstype ext4

services --enabled=NetworkManager --disabled=network,sshd
eula --agreed
firstboot --disable

#repo --name=base --baseurl=http://www.gtlib.gatech.edu/pub/scientific/7.4/x86_64/os/
#url --url="http://www.gtlib.gatech.edu/pub/scientific/7.4/x86_64/os/"

# Workaround for the grubby issue on live media (see https://bugzilla.redhat.com/show_bug.cgi?id=1153410)
# SL repositories (fastbugs enabled per default) 
#repo --name=security  --baseurl=http://ftp.scientificlinux.org/linux/scientific/7.4/$basearch/updates/security/ --excludepkgs=grubby

#repo --name=updates-fastbugs --baseurl=http://mirror.math.princeton.edu/pub/scientific/7.4/x86_64/updates/fastbugs/
#url --url="http://mirror.math.princeton.edu/pub/scientific/7.4/x86_64/updates/fastbugs/"



#repo --name=fedora-epel --baseurl=http://mirror.cs.princeton.edu/pub/mirrors/fedora-epel/7/x86_64
#url --url="http://mirror.cs.princeton.edu/pub/mirrors/fedora-epel/7/x86_64"


repo --name=base --baseurl=http://192.168.1.212/pub/scientific/7.4/x86_64/os
url --url="http://192.168.1.212/pub/scientific/7.4/x86_64/os"


# Workaround for the grubby issue on live media (see https://bugzilla.redhat.com/show_bug.cgi?id=1153410)
# SL repositories (fastbugs enabled per default) 
repo --name=local-updates-security  --baseurl=http://192.168.1.212/pub/scientific/7.4/$basearch/updates/security/ --excludepkgs=grubby
url --url="http://192.168.1.212/pub/scientific/7.4/x86_64/updates/security/"


repo --name=local-updates-fastbugs --baseurl=http://192.168.1.212/pub/scientific/7.4/x86_64/updates/fastbugs/
url --url="http://192.168.1.212/pub/scientific/7.4/x86_64/updates/fastbugs/"


#repo --name=fedora-epel --baseurl=http://mirror.cs.princeton.edu/pub/mirrors/fedora-epel/7/x86_64
#url --url="http://mirror.cs.princeton.edu/pub/mirrors/fedora-epel/7/x86_64"
repo --name=fedora-epel --baseurl=http://192.168.1.212/pub/fedora-epel/7/x86_64
url --url="http://192.168.1.212/pub/fedora-epel/7/x86_64"


#repo --name=elrepo-kernel --baseurl=http://dfw.mirror.rackspace.com/elrepo/kernel/el7/x86_64/
#url --url="http://dfw.mirror.rackspace.com/elrepo/kernel/el7/x86_64/"
repo --name=elrepo-kernel --baseurl=http://192.168.1.212/pub/elrepo/kernel/el7/x86_64
url --url="http://192.168.1.212/pub/elrepo/kernel/el7/x86_64"

#repo --name=rpmfusion --baseurl=http://download1.rpmfusion.org/free/el/updates/7/x86_64/
#url --url="http://download1.rpmfusion.org/free/el/updates/7/x86_64/"
repo --name=rpmfusion --baseurl=http://192.168.1.212/pub/rpmfusion/free/el/updates/7/x86_64/
url --url="http://192.168.1.212/pub/rpmfusion/free/el/updates/7/x86_64/"

repo --name=automyth_repo-desktop --baseurl=http://127.0.0.1/AutoMyth-Desktop-7.4/automyth_repo/Desktop
url --url="http://127.0.0.1/AutoMyth-Desktop-7.4/automyth_repo/Desktop"







# Workaround for the grubby issue on live media (see https://bugzilla.redhat.com/show_bug.cgi?id=1153410)
# SL repositories (fastbugs enabled per default) 
#repo --name=security  --baseurl=http://ftp.scientificlinux.org/linux/scientific/7.4/$basearch/updates/security/ --excludepkgs=grubby


%packages 
@base
@core
@fonts
@network-file-system-client
@print-client
@x11
gdm

#report problems 
abrt-desktop
#elrepo kernels
kernel-lt
# kernel-ml

hfsplus-tools
##fonts/icons misc

gnome-icon-theme-extras
gnome-icon-theme-legacy
gnome-icon-theme-symbolic
google-droid-sans-mono-fonts
liberation-sans-fonts
liberation-serif-fonts
liberation-mono-fonts
dejavu-sans-fonts
dejavu-sans-mono-fonts
dejavu-serif-fonts
gnome-backgrounds
f22-backgrounds


alsa-tools
alsa-plugins-oss

##xfce4
garcon
Thunar
xfwm4
xfce-bluetooth
xfdesktop
xfce4-appfinder
xfce4-mixer
xfce4-panel
xfce4-power-manager
xfce4-settings
xfce4-session
xfce4-terminal

xfce4-battery-plugin
xfce4-calculator-plugin
xfce4-cpufreq-plugin
xfce4-diskperf-plugin
xfce4-genmon-plugin
xfce4-netload-plugin
xfce4-notes-plugin
xfce4-screenshooter-plugin
xfce4-sensors-plugin
xfce4-systemload-plugin
xfce4-taskmanager
xfce4-time-out-plugin
xfce4-verve-plugin
xfce4-xkb-plugin
xarchiver
thunar-archive-plugin
redhat-menus

##webbrowser
chromium
mesa-libGL
mesa-libEGL
mesa-libGLES
mesa-libglapi
firefox
lynx

##office/mail
evince
evolution
libreoffice-calc
libreoffice-draw
libreoffice-impress
libreoffice-wiki-publisher
xpdf
Zim


##graphics
cheese
eog
shotwell
gimp
gthumb



##misc
alacarte
brasero
git
gnome-disk-utility
network-manager-applet
NetworkManager-bluetooth
#NetworkManager-strongswan-gnome
#NetworkManager-pptp-gnome
#NetworkManager-openconnect-gnome
#NetworkManager-libreswan-gnome
#NetworkManager-l2tp-gnome
#NetworkManager-vpnc-gnome
NetworkManager-openvpn-gnome
pulseaudio-module-bluetooth
system-config-date
system-config-language
system-config-keyboard
system-config-users

#database
mariadb


##mythtv
mythtv-docs
mythtv-frontend
#mythtv-setup
###mythtv-plugins
mytharchive
mythmusic
mythgallery
#mythweb
mythzoneminder

##terminals
tmux
terminator
xterm
xfce4-terminal

##livecd
#livecd-tools
anaconda

##misc av
audacity
cdrdao
gtk-recordmydesktop
hdhomerun
mplayer
pnmixer #works in tray for xfce
vlc
wodim


##misc apps
automythconkydesktop
dtv-scan-tables
dvb-apps
dvbsnoop
ffmpeg
gparted
motion
nmap
ntp
ntpdate
patch
scite
screen
system-storage-manager
Thunar
tigervnc
tree
scite
v4l-utils
vim-X11
wget
workrave
#xc3028L-firmware
libvirt-daemon
libvirt-daemon-kvm
libvirt-daemon-lxc
virt-manager

libnotify

#repos
elrepo-release
epel-release
rpmfusion-free-release

# for UEFI/Secureboot support
# extra ia32 for the 32bit bios/64bit cpu machines
efibootmgr
grub2
grub2-tools-extra
grub2-efi-ia32-cdboot
grub2-efi-x64-cdboot
shim-ia32
shim-unsigned-ia32
shim-x64
shim-unsigned-x64


-yum-cron
-initial-setup-gui
-initial-setup
%end

%post 
cat > /etc/rc.d/init.d/livesys << EOFLIVESYS1
#!/bin/bash
#
# live: Init script for live image
#
# chkconfig: 345 00 99
# description: Init script for live image.
### BEGIN INIT INFO
# X-Start-Before: display-manager
### END INIT INFO

. /etc/init.d/functions

if ! strstr "\`cat /proc/cmdline\`" rd.live.image || [ "\$1" != "start" ]; then
    exit 0
fi

if [ -e /.liveimg-configured ] ; then
    configdone=1
fi

exists() {
    which \$1 >/dev/null 2>&1 || return
    \$*
}

# Make sure we don't mangle the hardware clock on shutdown
ln -sf /dev/null /etc/systemd/system/hwclock-save.service

livedir="LiveOS"
for arg in \`cat /proc/cmdline\` ; do
  if [ "\${arg##rd.live.dir=}" != "\${arg}" ]; then
    livedir=\${arg##rd.live.dir=}
    return
  fi
  if [ "\${arg##live_dir=}" != "\${arg}" ]; then
    livedir=\${arg##live_dir=}
    return
  fi
done

# enable swaps unless requested otherwise
swaps=\`blkid -t TYPE=swap -o device\`
if ! strstr "\`cat /proc/cmdline\`" noswap && [ -n "\$swaps" ] ; then
  for s in \$swaps ; do
    action "Enabling swap partition \$s" swapon \$s
  done
fi
if ! strstr "\`cat /proc/cmdline\`" noswap && [ -f /run/initramfs/live/\${livedir}/swap.img ] ; then
  action "Enabling swap file" swapon /run/initramfs/live/\${livedir}/swap.img
fi

mountPersistentHome() {
  # support label/uuid
  if [ "\${homedev##LABEL=}" != "\${homedev}" -o "\${homedev##UUID=}" != "\${homedev}" ]; then
    homedev=\`/sbin/blkid -o device -t "\$homedev"\`
  fi

  # if we're given a file rather than a blockdev, loopback it
  if [ "\${homedev##mtd}" != "\${homedev}" ]; then
    # mtd devs don't have a block device but get magic-mounted with -t jffs2
    mountopts="-t jffs2"
  elif [ ! -b "\$homedev" ]; then
    loopdev=\`losetup -f\`
    if [ "\${homedev##/run/initramfs/live}" != "\${homedev}" ]; then
      action "Remounting live store r/w" mount -o remount,rw /run/initramfs/live
    fi
    losetup \$loopdev \$homedev
    homedev=\$loopdev
  fi

  # if it's encrypted, we need to unlock it
  if [ "\$(/sbin/blkid -s TYPE -o value \$homedev 2>/dev/null)" = "crypto_LUKS" ]; then
    echo
    echo "Setting up encrypted /home device"
    plymouth ask-for-password --command="cryptsetup luksOpen \$homedev EncHome"
    homedev=/dev/mapper/EncHome
  fi

  # and finally do the mount
  mount \$mountopts \$homedev /home
  # if we have /home under what's passed for persistent home, then
  # we should make that the real /home.  useful for mtd device on olpc
  if [ -d /home/home ]; then mount --bind /home/home /home ; fi
  [ -x /sbin/restorecon ] && /sbin/restorecon /home
  if [ -d /home/mythtv ]; then USERADDARGS="-M" ; fi
}

findPersistentHome() {
  for arg in \`cat /proc/cmdline\` ; do
    if [ "\${arg##persistenthome=}" != "\${arg}" ]; then
      homedev=\${arg##persistenthome=}
      return
    fi
  done
}

if strstr "\`cat /proc/cmdline\`" persistenthome= ; then
  findPersistentHome
elif [ -e /run/initramfs/live/\${livedir}/home.img ]; then
  homedev=/run/initramfs/live/\${livedir}/home.img
fi

# if we have a persistent /home, then we want to go ahead and mount it
if ! strstr "\`cat /proc/cmdline\`" nopersistenthome && [ -n "\$homedev" ] ; then
  action "Mounting persistent /home" mountPersistentHome
fi

# make it so that we don't do writing to the overlay for things which
# are just tmpdirs/caches
mount -t tmpfs -o mode=0755 varcacheyum /var/cache/yum
mount -t tmpfs vartmp /var/tmp
[ -x /sbin/restorecon ] && /sbin/restorecon /var/cache/yum /var/tmp >/dev/null 2>&1

if [ -n "\$configdone" ]; then
  exit 0
fi

# add live user passwd
action "Adding live user" useradd \$USERADDARGS -c "mythtv" mythtv
passwd -d mythtv > /dev/null
usermod -aG wheel mythtv > /dev/null
usermod -d /home/mythtv -s /bin/bash mythtv > /dev/null
grep mythtv /etc/passwd > /dev/null
usermod -c "mythtv" mythtv > /dev/null

# Remove root password lock
passwd -d root > /dev/null

# turn off firstboot for livecd boots
systemctl --no-reload disable firstboot-text.service 2> /dev/null || :
systemctl --no-reload disable firstboot-graphical.service 2> /dev/null || :
systemctl stop firstboot-text.service 2> /dev/null || :
systemctl stop firstboot-graphical.service 2> /dev/null || :

# don't use prelink on a running live image
sed -i 's/PRELINKING=yes/PRELINKING=no/' /etc/sysconfig/prelink &>/dev/null || :

# turn off mdmonitor by default
systemctl --no-reload disable mdmonitor.service 2> /dev/null || :
systemctl --no-reload disable mdmonitor-takeover.service 2> /dev/null || :
systemctl stop mdmonitor.service 2> /dev/null || :
systemctl stop mdmonitor-takeover.service 2> /dev/null || :

# don't enable the gnome-settings-daemon packagekit plugin
echo -e "\ngsettings set org.gnome.settings-daemon.plugins.updates active 'false' &>/dev/null || :" >> /home/mythtv/.bashrc

# don't start cron/at as they tend to spawn things which are
# disk intensive that are painful on a live image
systemctl --no-reload disable crond.service 2> /dev/null || :
systemctl --no-reload disable atd.service 2> /dev/null || :
systemctl stop crond.service 2> /dev/null || :
systemctl stop atd.service 2> /dev/null || :

# turn off libvirtd by default
systemctl --no-reload disable libvirtd.service 2> /dev/null || :
systemctl stop libvirtd.service 2> /dev/null || :



# and hack so that we eject the cd on shutdown if we're using a CD...
if strstr "\`cat /proc/cmdline\`" CDLABEL= ; then
  cat >> /sbin/halt.local << FOE
#!/bin/bash
# XXX: This often gets stuck during shutdown because /etc/init.d/halt
#      (or something else still running) wants to read files from the block\
#      device that was ejected.  Disable for now.  Bug #531924
# we want to eject the cd on halt, but let's also try to avoid
# io errors due to not being able to get files...
#cat /sbin/halt > /dev/null
#cat /sbin/reboot > /dev/null
#/usr/sbin/eject -p -m \$(readlink -f /run/initramfs/livedev) >/dev/null 2>&1
#echo "Please remove the CD from your drive and press Enter to finish restarting"
#read -t 30 < /dev/console
FOE
chmod +x /sbin/halt.local
fi


# disable kdump service
systemctl --no-reload disable kdump.service 2> /dev/null || :
systemctl stop kdump.service 2> /dev/null || :

# disable tuned.service
systemctl --no-reload disable tuned.service 2> /dev/null || :
systemctl stop tuned.service 2> /dev/null || :

# Mark things as configured
touch /.liveimg-configured

# add static hostname to work around xauth bug
# https://bugzilla.redhat.com/show_bug.cgi?id=679486
echo localhost.localdomain > /etc/hostname


# Fixing the lang install issue when other lang than English is selected . See http://bugs.centos.org/view.php?id=7217
/usr/bin/cp /usr/lib/python2.7/site-packages/blivet/size.py /usr/lib/python2.7/site-packages/blivet/size.py.orig
/usr/bin/sed -i "s#return self.humanReadable()#return self.humanReadable().encode('utf-8')#g" /usr/lib/python2.7/site-packages/blivet/size.py

EOFLIVESYS1


# bah, hal starts way too late
cat > /etc/rc.d/init.d/livesys-late << EOFLIVESYSLATE
#!/bin/bash
#
# live: Late init script for live image
#
# chkconfig: 345 99 01
# description: Late init script for live image.

. /etc/init.d/functions

if ! strstr "\`cat /proc/cmdline\`" rd.live.image || [ "\$1" != "start" ] || [ -e /.liveimg-late-configured ] ; then
    exit 0
fi

exists() {
    which \$1 >/dev/null 2>&1 || return
    \$*
}

touch /.liveimg-late-configured

# read some variables out of /proc/cmdline
for o in \`cat /proc/cmdline\` ; do
    case \$o in
    ks=*)
        ks="--kickstart=\${o#ks=}"
        ;;
    xdriver=*)
        xdriver="\${o#xdriver=}"
        ;;
    esac
done

# if liveinst or textinst is given, start anaconda
if strstr "\`cat /proc/cmdline\`" liveinst ; then
   plymouth --quit
   /usr/sbin/liveinst \$ks
fi
if strstr "\`cat /proc/cmdline\`" textinst ; then
   plymouth --quit
   /usr/sbin/liveinst --text \$ks
fi

# configure X, allowing user to override xdriver
if [ -n "\$xdriver" ]; then
   cat > /etc/X11/xorg.conf.d/00-xdriver.conf <<FOE-XOVERRIDE-LIVESYS-LATE
Section "Device"
	Identifier	"Videocard0"
	Driver	"\$xdriver"
EndSection
FOE-XOVERRIDE-LIVESYS-LATE
fi

EOFLIVESYSLATE


chmod 755 /etc/rc.d/init.d/livesys
/sbin/restorecon /etc/rc.d/init.d/livesys
/sbin/chkconfig --add livesys

chmod 755 /etc/rc.d/init.d/livesys-late
/sbin/restorecon /etc/rc.d/init.d/livesys-late
/sbin/chkconfig --add livesys-late

# enable tmpfs for /tmp
systemctl enable tmp.mount

# work around for poor key import UI in PackageKit
rm -f /var/lib/rpm/__db*
releasever=$(rpm -q --qf '%{version}\n' --whatprovides system-release)
basearch=$(uname -i)
rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-*
echo "Packages within this LiveCD (with version)"
rpm -qa | sort
echo "Packages within this LiveCD"
rpm -qa --qf "%{name}\n" | sort
# Note that running rpm recreates the rpm db files which aren't needed or wanted
rm -f /var/lib/rpm/__db*

# go ahead and pre-make the man -k cache (#455968)
/usr/bin/mandb

# save a little bit of space at least...
rm -f /boot/initramfs*
# make sure there aren't core files lying around
rm -f /core*


# convince readahead not to collect
# FIXME: for systemd

# set chromium browsers home and startpages 
sed -i 's|"https://start.fedoraproject.org"|"file:///home/mythtv/AutoMyth-Desktop-7.4/ReadmeFirst.txt"|g' /etc/chromium/master_preferences

cat >> /etc/rc.d/init.d/livesys << EOFLIVESYS2

#activate conky
cp -avf /etc/conky/automyth.conky.conf /home/mythtv/.conkyrc
#other users get .conkyrc from the conky rpm (etc/skel/.conkyrc)
#activate autostart of conky and updateConkyrc
mkdir -m 0700 -pv /home/mythtv/.config/autostart
cp -avf /usr/share/applications/conky.desktop /home/mythtv/.config/autostart/
cp -avf /usr/share/applications/updateConkyrc.desktop /home/mythtv/.config/autostart/
chmod 0755 -R /home/mythtv/.config/autostart/*.desktop
chown mythtv. /home/mythtv/.conkyrc



# Show harddisk install on the desktop
sed -i -e 's/NoDisplay=true/NoDisplay=false/' /usr/share/applications/liveinst.desktop
mkdir /home/mythtv/Desktop
cp -av /usr/share/applications/mythfrontend.desktop /home/mythtv/Desktop/
cp -av /usr/share/applications/gparted.desktop /home/mythtv/Desktop/
cp -av /usr/share/applications/liveinst.desktop /home/mythtv/Desktop/
# and mark it as executable (new Xfce security feature)
chmod +x /home/mythtv/Desktop/liveinst.desktop
chmod +x /home/mythtv/Desktop/gparted.desktop
chmod +x /home/mythtv/Desktop/mythfrontend.desktop
chown -R mythtv:mythtv /home/mythtv

## disable updates plugin
cat >> /usr/share/glib-2.0/schemas/org.gnome.settings-daemon.plugins.updates.gschema.override << FOE
[org.gnome.settings-daemon.plugins.updates]
active=false
FOE

# Show the system-config-keyboard tool on the desktop
mkdir /home/mythtv/Desktop -p >/dev/null
cat /usr/share/applications/system-config-keyboard.desktop | sed '/NotShowIn/d' |sed 's/Terminal=false/Terminal=true/' > /home/mythtv/Desktop/system-config-keyboard.desktop
cat /usr/share/applications/liveinst.desktop | sed '/NoDisplay/d' > /home/mythtv/Desktop/liveinst.desktop
chmod +x /home/mythtv/Desktop/*.desktop
chown -R mythtv. /home/mythtv

# Liveuser face
if [ -e /usr/share/icons/hicolor/96x96/apps/fedora-logo-icon.png ] ; then
    cp /usr/share/icons/hicolor/96x96/apps/fedora-logo-icon.png /home/mythtv/.face
    chown mythtv. /home/mythtv/.face
fi

# make the installer show up
if [ -f /usr/share/applications/liveinst.desktop ]; then
  # Show harddisk install in shell dash
  sed -i -e 's/NoDisplay=true/NoDisplay=false/' /usr/share/applications/liveinst.desktop 
  # need to move it to anaconda.desktop to make shell happy
  #cp /usr/share/applications/liveinst.desktop /usr/share/applications/anaconda.desktop
fi

cat > /etc/sysconfig/desktop <<FOE
PREFERRED=/usr/bin/startxfce4
DISPLAYMANAGER=/usr/sbin/gdm
FOE


# Turn off PackageKit-command-not-found while uninstalled
#if [ -f /etc/PackageKit/CommandNotFound.conf ]; then
#  sed -i -e 's/^SoftwareSourceSearch=true/SoftwareSourceSearch=false/' /etc/PackageKit/CommandNotFound.conf
#fi

# make sure to set the right permissions and selinux contexts
chmod +x /home/mythtv/.xinitrc
chown -R mythtv. /home/mythtv/*
chmod 0755 -R /home/mythtv/*
chown -R mythtv. /home/mythtv/*

chmod 0755 -R /home/mythtv/*
chown -R mythtv. /home/mythtv/*

/sbin/restorecon -R /home/mythtv/
/sbin/restorecon -R /myth

# Fixing default locale to us - does not work for SL7.1
#localectl set-keymap us
#localectl set-x11-keymap us

EOFLIVESYS2

# rebuild schema cache with any overrides we installed
glib-compile-schemas /usr/share/glib-2.0/schemas
####################

mkdir /home/mythtv
chown -R mythtv:mythtv /home/mythtv


###REMOVE BEFORE RELEASE
#add mythtv account to wheel for sudo
/usr/sbin/usermod -aG wheel mythtv
sed -i "s|.*# %wheel|%wheel|" /etc/sudoers

#set mythtv account to my specs
#sed -i "s|mythtv user:/var/lib/mythtv:/sbin/nologin|mythtv:/home/mythtv:/bin/bash|" /etc/passwd
cat >> /etc/sudoers << FOE-SUDOERS
%mythtv ALL=(ALL)	NOPASSWD: ALL
FOE-SUDOERS


#set some custom xterm options
cat >> /home/mythtv/.Xdefaults << FOE-XDEFAULTS
XTerm.vt100.reverseVideo: true
XTerm.vt100.faceName: Liberation Mono:size=10:antialias=false
XTerm.vt100.font: 7x13
XTerm.*scrollBar: true
XTerm.*scrollTtyOutput: false
#XTerm.vt100.geometry: 100x45-0-0
FOE-XDEFAULTS

#activate conky
cp -avf /etc/conky/automyth.conky.conf /home/mythtv/.conkyrc
#other users get .conkyrc from the conky rpm (etc/skel/.conkyrc)
#activate autostart of conky and updateConkyrc
mkdir -m 0700 -pv /home/mythtv/.config/autostart
cp -avf /usr/share/applications/conky.desktop /home/mythtv/.config/autostart/
cp -avf /usr/share/applications/updateConkyrc.desktop /home/mythtv/.config/autostart/
chmod 0644 -R /home/mythtv/.config/autostart/*.desktop



##
mkdir -pv /home/mythtv/.config/xfce4/terminal

# disable screensaver locking (#674410)
cat >> /home/mythtv/.xscreensaver << FOE
mode:           off
lock:           True
dpmsEnabled:    True
FOE


# deactivate xfconf-migration (#683161)
rm -f /etc/xdg/autostart/xfconf-migration-4.6.desktop || :

# deactivate xfce4-panel first-run dialog (#693569)
mkdir -p /home/mythtv/.config/xfce4/xfconf/xfce-perchannel-xml
cp /etc/xdg/xfce4/panel/default.xml /home/mythtv/.config/xfce4/xfconf/xfce-perchannel-xml/xfce4-panel.xml

# amdevs# This fails partially
# Patching livepayload too https://bugzilla.redhat.com/show_bug.cgi?id=1100504
cat > /tmp/livepayload.patch <<EOF
--- /usr/lib64/python2.7/site-packages/pyanaconda/packaging/livepayload.py.orig	
+++ /usr/lib64/python2.7/site-packages/pyanaconda/packaging/livepayload.py
@@ -152,6 +152,13 @@
             self.pct = 100
         threadMgr.wait(THREAD_LIVE_PROGRESS)
 
+        # Live needs to create the rescue image before bootloader is written
+        for kernel in self.kernelVersionList:
+            log.info("Generating rescue image for %s", kernel)
+            iutil.execInSysroot("new-kernel-pkg",
+                                ["--rpmposttrans", kernel])
+
+
     def postInstall(self):
         """ Perform post-installation tasks. """
         progressQ.send_message(_("Performing post-installation setup tasks"))
@@ -159,12 +166,6 @@
 
         super(LiveImagePayload, self).postInstall()
 
-        # Live needs to create the rescue image before bootloader is written
-        for kernel in self.kernelVersionList:
-            log.info("Generating rescue image for %s", kernel)
-            iutil.execInSysroot("new-kernel-pkg",
-                                ["--rpmposttrans", kernel])
-
         # Make sure the new system has a machine-id, it won't boot without it
         if not os.path.exists(iutil.getSysroot()+"/etc/machine-id"):
             iutil.execInSysroot("systemd-machine-id-setup", [])

EOF

patch -p0 /usr/lib64/python2.7/site-packages/pyanaconda/packaging/livepayload.py /tmp/livepayload.patch 


mkdir -pv /home/mythtv/.local/share/applications
touch /home/mythtv/.local/share/applications/mimeapps.list
chown -R mythtv. /home/mythtv/.local


# set up auto-login
cat > /etc/gdm/custom.conf << FOE
[daemon]
AutomaticLoginEnable=True
AutomaticLogin=mythtv
FOE

#set terminal config
cat > /home/mythtv/.config/xfce4/terminal/terminalrc << FOE
[Configuration]
FontName=Monospace 10
MiscAlwaysShowTabs=FALSE
MiscBell=FALSE
MiscBordersDefault=TRUE
MiscCursorBlinks=FALSE
MiscCursorShape=TERMINAL_CURSOR_SHAPE_BLOCK
MiscDefaultGeometry=80x24
MiscInheritGeometry=FALSE
MiscMenubarDefault=TRUE
MiscMouseAutohide=FALSE
MiscToolbarDefault=FALSE
MiscConfirmClose=TRUE
MiscCycleTabs=TRUE
MiscTabCloseButtons=TRUE
MiscTabCloseMiddleClick=TRUE
MiscTabPosition=GTK_POS_TOP
MiscHighlightUrls=TRUE
FOE

echo "localhost.localdomain" > /etc/hostname


#disable ssh for mythtv by default
grep -x "DenyUsers mythtv" /etc/ssh/sshd_config 2> /dev/null
if [ $? -eq 0 ];then
       echo "already exists in /etc/ssh/sshd_config"
else
       echo "DenyUsers mythtv" >>/etc/ssh/sshd_config
fi

#include the source/scripts in the install image
cd /home/mythtv
mkdir AutoMyth-Desktop-7.4;cd AutoMyth-Desktop-7.4

#Source
wget http://127.0.0.1/AutoMyth-Desktop-7.4/ReadmeFirst.txt
wget http://127.0.0.1/AutoMyth-Desktop-7.4/automythdesktop-0.28.1-62-g36f.cfg
wget http://127.0.0.1/AutoMyth-Desktop-7.4/createNewAutomythdesktopImage-0.28.1-62-g36fe0ce.sh
wget --cut-dirs=1 --no-parent --no-host-directories --reject=index.html*  --mirror http://127.0.0.1/AutoMyth-Desktop-7.4/automyth_repo/ >/dev/null

#RPMME
mkdir -pv /usr/share/backgrounds/images
cp -avf /home/mythtv/AutoMyth-Desktop-7.4/default.png /usr/share/backgrounds/images/
echo mythtv|passwd --stdin mythtv > /dev/null
chmod 0755 -R /home/mythtv/AutoMyth-Desktop-7.4/*
chown -R mythtv. /home/mythtv/.config*
#chown -R mythtv. /home/mythtv/.mythtv*
chown -R mythtv. /home/mythtv/.local*
chown  mythtv. /home/mythtv/.xscreensaver
chown  mythtv. /home/mythtv/.conkyrc

#liveinst clean self remove utils /scripts
cat > /root/.clean_after_liveinst.sh <<EOF-CLEANLIVEINST
#!/bin/bash
if [ ! -e /dev/mapper/live-rw ];then
		rpm -qa|grep anaconda|xargs rpm -e
		sleep 2
		rm -rfv /var/spool/cron/root
		rm $0
fi
EOF-CLEANLIVEINST

chmod +x /root/.clean_after_liveinst.sh

#setup the cron job to exec it
cat >/var/spool/cron/root<<EOF-ROOTCRON
@reboot     /root/.clean_after_liveinst.sh
EOF-ROOTCRON

%end


%post --nochroot
cp $INSTALL_ROOT/usr/share/doc/*-release-*/GPL $LIVE_ROOT/GPL

# only works on x86, x86_64
if [ "$(uname -i)" = "i386" -o "$(uname -i)" = "x86_64" ]; then
  if [ ! -d $LIVE_ROOT/LiveOS ]; then mkdir -p $LIVE_ROOT/LiveOS ; fi
  cp /usr/bin/livecd-iso-to-disk $LIVE_ROOT/LiveOS
fi

%end
